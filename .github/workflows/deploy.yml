name: Docker Build & Publish

on:
  push:
    branches:
      - main
      - master

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        run: docker login -u "${{ secrets.DOCKER_USERNAME }}" -p "${{ secrets.DOCKER_PASSWORD }}"

      - name: Build Docker Image
        run: docker build -t /store-api:${{ github.sha }} .

      - name: Tag Latest
        run: docker mariguapo/store-api:${{ github.sha }} mariguapo/store-api:latest

      - name: Push Docker Image
        run: |
          docker push mariguapo/store-api:${{ github.sha }}
          docker push mariguapo/store-api:latest
  
  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to AWS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            docker pull mariguapo/store-api:latest
            cd containers
            docker compose up -d